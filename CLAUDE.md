# Fork notice

This checkout is a personal development fork of herdrdev/herdr, not the
upstream repository. Read `FORK.md` before doing anything else. It explains
the goal, the ground rules, what Herdr already provides, prior art, the
current design direction, and the status log.

The engineering rules in `AGENTS.md` (architecture principles, performance
paths, protocol contracts, code conventions, testing) still apply to code
written here. The maintainer workflow, local machine, release channel, and
external contributor sections of `AGENTS.md` describe upstream's process and
do not govern this fork. Never push to `upstream`; it is fetch-only on purpose.

@FORK.md
@AGENTS.md
